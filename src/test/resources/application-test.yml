spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=20000
app:
  seed-demo-data: false
  scheduling:
    enabled: false        # tests trigger sweeper / reminders explicitly
  hold:
    duration-minutes: 5
  booking:
    max-seats-per-booking: 6
